import MapPageClient from '@/components/MapPage/MapPageClient';

export const metadata = {
  title: 'Harita — AquaGuard',
  description: 'AquaGuard interaktif su kalitesi haritası.',
};

export default function HaritaPage() {
  return <MapPageClient />;
}
